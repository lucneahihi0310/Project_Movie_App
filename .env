# ====== SERVER CƠ BẢN ======
# Port backend Express (phải trùng với server.js)
PORT=5000

# Đường dẫn DB (nếu muốn cấu hình động, hiện tại đang dùng path.join nên có thể bỏ qua)
# DATABASE_PATH=./database.json


# ====== CLOUDINARY (Upload ảnh) ======
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret


# ====== RESEND (Gửi email) ======
RESEND_API_KEY=your_resend_api_key
# Email đã verify trong Resend (From)
FROM_EMAIL=no-reply@yourdomain.com
# (Tuỳ chọn) Email nhận thông báo, nếu khác FROM_EMAIL
TO_EMAIL=you@yourdomain.com


# ====== VNPAY (Thanh toán online) ======
# Lấy từ trang quản trị VNPay sandbox/production
VNPAY_TMN_CODE=7HJM21XJ
VNPAY_HASH_SECRET=EUGNBYHOEDDNGAR4NW90DXOGTIXGS26I

# URL cổng VNPAY (sandbox hoặc production)
VNPAY_URL=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html

# URL backend nhận kết quả thanh toán từ trình duyệt (return URL)
VNPAY_RETURN_URL=http://localhost:5000/vnpay/return

# URL backend nhận IPN từ VNPay (thường giống domain backend)
VNPAY_IPN_URL=http://localhost:5000/vnpay/ipn

# (Legacy) URL cũ /payment-result nếu bạn vẫn dùng
VNPAY_LEGACY_RETURN_URL=http://localhost:5000/payment-result


# ====== FRONTEND (CRA) ======
# URL backend để frontend gọi API
REACT_APP_API_BASE_URL=http://localhost:5000

# (Tuỳ chọn) URL frontend, dùng cho redirect trong VNPay (hiện đang hard-code http://localhost:3000)
# FRONTEND_URL=http://localhost:3000